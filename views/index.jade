extends layout
block content
    .container
        .row
            .col-md-6.col-md-offset-3
                h1.h1= title 
                    Small  wellcome to #{title}
                form.form-horizontal(name="login", action="/admin", method="post")
                    .form-group
                        input.form-control(type="text", ng-model="username", placeholder="email")
                    .form-group
                        input.form-control(type="password", ng-model="password" placeholder="password")
                    .form-group
                        button.bnt.btn-primary(ng-click="{username: password}" type="submit", value="login") log in
                a(href='#') Forgot password?